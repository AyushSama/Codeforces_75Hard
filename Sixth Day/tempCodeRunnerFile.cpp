;
    // int minn = INT_MAX;
    // auto temp = pq.top();
    // pq.pop();
    // minn = min(minn,temp.first);
    // while(!pq.empty()){
    //     temp = pq.top();
    //     pq.pop();
    //     minn = min(minn,temp.first);
    //     res+= temp.second;
    // }
    // res+= minn;